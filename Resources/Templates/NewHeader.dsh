// {FILENAME} -- a DreamShader header: functions shared by the materials that import it.
// Import it from a .dsm with:  import "{FILENAME}";
Function float Luma(in vec3 color)
{
    return dot(color, float3(0.299, 0.587, 0.114));
}
